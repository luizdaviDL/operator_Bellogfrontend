import React from 'react'

const Success = ({text}) => {
  return (
    <div>
      <div class="alert alert-success" role="alert">
            {text}
        </div>
    </div>
  )
}

export default Success
